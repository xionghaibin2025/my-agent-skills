# Core (almost every figure)
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
scipy>=1.10

# Statistics / models (use when the analysis needs them)
statsmodels>=0.14
pingouin>=0.5          # convenient stats + assumption checks
statannotations>=0.6   # significance brackets on seaborn plots

# Optional plotting stacks (install as needed)
plotnine>=0.13         # grammar of graphics (ggplot-style)
plotly>=5.20           # interactive HTML figures
kaleido>=0.2           # static image export from plotly

# Specialized figures
lifelines>=0.27        # Kaplan-Meier / Cox survival analysis
scikit-learn>=1.3      # PCA / clustering / dimensionality reduction (e.g. PCA scatter)

# Table export (when delivering .xlsx/.docx tables)
openpyxl>=3.1          # read/write Excel
python-docx>=1.1       # Word tables

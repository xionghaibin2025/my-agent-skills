# China Mining Magazine — Gakedaban ΔT isogram

This gallery case publishes only visible raster evidence. `data.csv` summarises the 25 legend colour classes; `visible-contour-band-grid.npz` retains the per-pixel class grid used by the static recreation. The full recreation preserves the source-raster legend and other figure furniture outside the recovered map polygon. It does not represent original survey points, an author grid, or interpolation parameters.
